var e='---\ntitle: "Forms, Tables + Media"\nduration: 90\nlevel: 1\nlevelLabel: "LVL 1"\nlevelColor: "#6ee7b7"\nvideos:\n  - id: "UB1O30fR-EE"\n    title: "HTML Crash Course For Absolute Beginners — Traversy Media"\n  - id: "kUMe1FH4CHE"\n    title: "Learn HTML – Full Tutorial for Beginners — freeCodeCamp"\nquiz:\n  type: quick-check\n  passMark: 60\n  questions:\n    - type: multiple-choice\n      question: "Why is the \'for\' attribute on a <label> important?"\n      options: ["It tells the form where to submit", "It links the label to its input so clicking the label focuses the input", "It validates the input", "It\'s required for forms to work"]\n      answer: 1\n      explanation: "The \'for\' attribute value must match the \'id\' of the input. This links them — clicking the label focuses the input, which is especially helpful for checkboxes and screen readers."\n    - type: multiple-choice\n      question: "What is the \'alt\' attribute on an <img> tag used for?"\n      options: ["Alternative styling", "Text shown when the image fails to load, and read by screen readers", "Setting the image size", "Linking to another image"]\n      answer: 1\n      explanation: "alt text is critical for accessibility — screen readers read it aloud. It also appears if the image URL is broken. Every <img> must have an alt attribute."\n    - type: fill-blank\n      question: "The HTML input type for an email address field is ___"\n      answer: "type=\\"email\\""\n      explanation: "type=\'email\' triggers email keyboard on mobile and adds basic format validation in modern browsers."\n---\n\n## Phase 1 — Learn (40 min)\n\n**Why this matters:** Forms are how users send data to your application — search queries, login credentials, contact messages, file uploads. Getting forms right (including accessibility) is one of the most impactful skills in web development. Tables are for tabular data, not layout.\n\n- **Form structure** — `<form>` wraps all inputs. `action` is where data goes on submit. `method` is `GET` (data in URL) or `POST` (data in body).\n- **Input types** — `type="text"` (generic text), `type="email"` (validates email format), `type="number"` (numeric keyboard), `type="password"` (masked), `type="checkbox"` (true/false), `type="radio"` (one of many).\n- **Label and for** — `<label for="name">Name</label><input id="name">` — the `for` value matches the input\'s `id`. Clicking the label focuses the input. Required for accessibility.\n- **Textarea** — `<textarea rows="4" name="message">` for multi-line text. Has opening and closing tags unlike `<input>`.\n- **Select and option** — `<select>` creates a dropdown. Each choice is an `<option value="value">Label</option>`.\n- **Button types** — `type="submit"` submits the form, `type="button"` does nothing by default (useful for JavaScript), `type="reset"` clears the form.\n- **Images** — `<img src="path.jpg" alt="Description" width="300">`. `alt` is mandatory for accessibility. `width`/`height` prevent layout shifts.\n- **Tables** — for data that has rows and columns. `<table>` → `<thead>`/`<tbody>` → `<tr>` (row) → `<th>` (header cell) or `<td>` (data cell).\n\n> **Key insight:** Never use tables for layout — that was a 1990s hack. Tables are for data like spreadsheets or schedules. Use CSS Grid or Flexbox for page layout.\n\n## Phase 2 — Do (40 min)\n\n- Add a contact form to the bio page from Session 01 inside a `<section id="contact">`:\n  - Name field (text), Email field (email), Subject field (text), Message (textarea), Submit button\n  - Every input must have a properly linked `<label>`\n  - Add a `required` attribute to all fields\n- Add one image with a meaningful `alt` attribute\n- Style the form in CSS: add spacing between fields, style the button with a background colour and border-radius\n\n## Review\n\nWhat happens when you submit a form with `method="GET"` vs `method="POST"`? Open DevTools Network tab and observe the difference.\n';export{e as default};